[project]
name = "ai-question-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rohith", email = "leo433554@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32.5",
]

[project.scripts]
ai-question-analyzer = "ai_question_analyzer:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
